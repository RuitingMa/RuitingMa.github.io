---
import SketchLayout from '../../layouts/SketchLayout.astro';
import ShiSketch from '../../components/ShiSketch.astro';
import { isDraft } from '../../lib/sketches';

if (import.meta.env.PROD && isDraft('shi')) return Astro.redirect('/404');
---

<SketchLayout slug="shi" stageHeightMobile="62svh">
  <ShiSketch slot="stage" />

  <p class="note enter" style="--i: 3;">
    以手写之——任何一位数字。光标落下的那一帧，上方二十八见方的笔迹被展成七百八十四维向量，径直喂给一副极小的多层感知机：两层十六个神经元，外加十类输出。每一根墨迹都与其下的隐层相连，线即权重，色即符号——苔绿为正，藕荷为负，亮度正比于此刻实际流过的信号。
  </p>
  <p class="note enter" style="--i: 4;">
    这一副网络没有任何关于平移的先验，也没有把笔迹归中到画框的内置步骤。所以它只认一种姿态：那种 MNIST 训练集里规范好的、端端正正坐在正中的写法。若把字偏往角落、写得过大或过小，线网会乱成另一幅图样，它会把一横认作七、把斜的七认作一，一视同仁地自信。这一点也让它能成立——识字从来是对位置的一种偏见。
  </p>
</SketchLayout>
